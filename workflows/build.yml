name: Build Team React App


on:
  # push:
  #   branches: [main]  

  workflow_dispatch:




jobs:
  Build:

    runs-on: self-hosted

    steps:

      - name: Checkout Repo
        uses: actions/checkout@v5.0.0
        
      - name: Setup Node JS
        
      
